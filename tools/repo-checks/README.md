# Repository Checks

Run the documentation harness check from the repository root:

```bash
python3 tools/repo-checks/check_docs.py
```

The check validates required agent entry points, phase plans, domain maps, repo-specific skill drafts, and common placeholder markers in documentation.
